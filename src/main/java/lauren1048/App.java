/**
*Lauren Lira - lllira1048
*CIT 4423 01
*Nov 13,2022
*Windows 11
*/

package lauren1048;

import javax.swing.JFrame;

/**
 * Hello world!
 * t
 */
public class App {/*Important! This program will take a few seconds to load, and also takes a little bit when the back to store button is pressed. 
    This is designed to be a store program in order to purchase items. The user may select and add 
    items to a receipt, and then edit the receipt or return back to the online shop. Confirming the order exits the 
    system.  */
public static void main(String[] args) {

        // for (int i = 0; i < 1000; i++) {/*
        // * Meant to run once in order to build SQL
        // * database
        // */
        // CustomerData customerdata = new CustomerData();
        // customerdata.persistData();
        // }

        // for (int i = 0; i < 1000; i++) {/*
        // * Meant to run once in order to build SQL
        // * database
        // */
        // ProductData productdata = new ProductData();
        // productdata.persistProduct();
        // }

        UI ui = new UI();// Calls user interface

    }
}// End of Main
